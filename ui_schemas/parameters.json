{
    "type": "object",
    "properties": {
        "kelvin_closed_loop": {
            "type": "boolean",
            "title": "Kelvin Closed Loop"
        },
        "temperature_max_threshold": {
            "type": "number",
            "title": "Temperature Max Threshold",
            "minimum": 50,
            "maximum": 100
        }
    },
    "required": [
        "kelvin_closed_loop",
        "temperature_max_threshold"
    ]
}