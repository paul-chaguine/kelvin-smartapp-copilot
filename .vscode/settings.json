{
  "multiCommand.commands": [
    {
      "command": "multiCommand.saveAndPush",
      "sequence": [
        "workbench.action.files.save",
        { "command": "workbench.action.tasks.runTask", "args": "Stage Commit Push" }
      ]
    }
  ]
}
